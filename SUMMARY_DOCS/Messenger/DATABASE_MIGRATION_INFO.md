# 🗄️ Информация о миграциях БД

## Безопасность миграций для существующих пользователей

### ✅ Краткий ответ

**НЕТ, миграции НЕ сломают регистрацию и сообщения!**

Все миграции в этом коммите **полностью безопасны**:
- ✅ Существующие пользователи сохраняются
- ✅ Все сообщения и чаты сохраняются
- ✅ Регистрация и вход работают без изменений
- ✅ Добавлены только опциональные поля

---

## 📋 Детали миграций

### Миграция 1: `20260430130442_add_bio_field`

**Что делает:**
```sql
-- Добавляет поле bio (текст о себе)
ALTER TABLE "User" ADD COLUMN "bio" TEXT;
```

**Безопасность:**
- ✅ Поле `bio` является **опциональным** (nullable)
- ✅ Все существующие пользователи получают `NULL` в этом поле
- ✅ Нет изменений в обязательных полях (email, passwordHash, displayName)
- ✅ Нет изменений в связях (messages, chats)

**Влияние на пользователей:**
- 🟢 **Существующие пользователи**: ничего не меняется, работают как прежде
- 🟢 **Новые пользователи**: могут заполнить bio при желании
- 🟢 **Регистрация**: работает без изменений

---

### Миграция 2: `20260430131150_add_settings_field`

**Что делает:**
```sql
-- Добавляет поле settings (JSON для настроек профиля)
ALTER TABLE "User" ADD COLUMN "settings" JSONB;
```

**Безопасность:**
- ✅ Поле `settings` является **опциональным** (nullable)
- ✅ Все существующие пользователи получают `NULL` в этом поле
- ✅ Хранит только пользовательские настройки (тема, язык, уведомления)
- ✅ Не влияет на аутентификацию

**Влияние на пользователей:**
- 🟢 **Существующие пользователи**: ничего не меняется
- 🟢 **Новые пользователи**: могут использовать для хранения настроек
- 🟢 **Вход/выход**: работает без изменений

---

### Миграция 3: Исправление индексов (Contact, FamilyRelation)

**Что делает:**
```sql
-- Добавляет уникальный индекс для Contact
CREATE UNIQUE INDEX "userId_contactId" ON "Contact"(userId, contactId);

-- Добавляет уникальный индекс для FamilyRelation  
CREATE UNIQUE INDEX "userId_relatedUserId" ON "FamilyRelation"(userId, relatedUserId);
```

**Безопасность:**
- ✅ Только добавление индексов (не меняет данные)
- ✅ Ускоряет запросы по контактам
- ✅ Нет изменений в структуре таблиц

**Влияние на пользователей:**
- 🟢 **Все пользователи**: работают как прежде
- 🟢 **Производительность**: немного улучшается
- 🟢 **Функциональность**: без изменений

---

## 🔍 Что НЕ изменено

### ❌ НЕТ изменений в:
- `email` - email пользователя
- `passwordHash` - хеш пароля
- `displayName` - отображаемое имя
- `id` - ID пользователя
- `createdAt` - дата регистрации
- `messages` - таблицы сообщений
- `chats` - таблицы чатов
- `ChatMember` - участники чатов
- `MessageReaction` - реакции на сообщения
- `Invitation` - приглашения

### ❌ НЕТ изменений в логике:
- ✅ Регистрация новых пользователей
- ✅ Вход/выход существующих пользователей
- ✅ Отправка/получение сообщений
- ✅ Создание/удаление чатов
- ✅ Управление контактами
- ✅ Система приглашений

---

## 🛡️ Рекомендации перед миграцией

### 1. Создайте резервную копию БД

```bash
# Остановите приложение
pm2 stop messenger-alpha

# Создайте резервную копию
cp messenger/prisma/dev.db messenger/prisma/dev.db.backup.$(date +%Y%m%d_%H%M%S)

# Проверьте что файл создан
ls -lh messenger/prisma/dev.db*
```

### 2. Протестируйте на staging (если есть)

```bash
# Скопируйте production БД на staging
# Запустите миграции на staging
# Проверьте что все работает
```

### 3. Используйте безопасный скрипт деплоя

```bash
# Скрипт автоматически создает бэкап перед миграцией
bash SAFE_DEPLOY.sh
```

---

## 🔄 Откат миграции (если нужно)

Если возникнут проблемы:

```bash
# 1. Остановите приложение
pm2 stop messenger-alpha

# 2. Восстановите резервную копию
cp messenger/prisma/dev.db.backup.YYYYMMDD_HHMMSS messenger/prisma/dev.db

# 3. Запустите приложение
pm2 start messenger-alpha

# 4. Проверьте работу
pm2 logs messenger-alpha
curl http://localhost:3000
```

---

## 📊 Статистика миграций

| Миграция | Тип | Влияние на данные | Откат |
|----------|-----|-------------------|-------|
| add_bio_field | ADD COLUMN | ✅ Нет | ✅ Легкий |
| add_settings_field | ADD COLUMN | ✅ Нет | ✅ Легкий |
| fix_contact_index | ADD INDEX | ✅ Нет | ✅ Легкий |
| fix_relation_index | ADD INDEX | ✅ Нет | ✅ Легкий |

---

## ✅ Итог

**Миграции 100% безопасны для существующих пользователей:**

- ✅ Все данные сохраняются
- ✅ Регистрация работает
- ✅ Сообщения работают
- ✅ Чаты работают
- ✅ Вход/выход работает
- ✅ Нет риска потери данных

**Рекомендуется:**
1. Сделать бэкап БД (1 минута)
2. Запустить миграции (2-3 минуты)
3. Проверить работу приложения

---

*Документация обновлена: 2025*
