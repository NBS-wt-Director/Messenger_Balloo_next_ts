# Infrastructure

Infrastructure configuration for the Balloo platform.

## Structure

- `nginx/` - Nginx configuration
- `docker/` - Docker configuration
- `deploy/` - Deployment scripts

## Rules

1. **Infra-as-code**: All infrastructure in code
2. **Environment-aware**: Separate configs per environment
3. **Secure**: No secrets in repo
