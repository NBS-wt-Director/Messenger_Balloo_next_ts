# Workspace-safe defaults
# Does not break existing npm workflow

# Enable pnpm workspace (optional, safe default)
# Can be overridden per-project

# Safe hoisting
hoist=false

# Do not auto-install peer dependencies
auto-install-peers=false

# Safe symlink mode
shamefully-hoist=false

# Allow build scripts
enable-pre-post-scripts=true
