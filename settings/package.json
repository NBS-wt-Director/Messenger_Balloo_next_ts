{
  "name": "@app-balloo/settings",
  "version": "1.0.0",
  "description": "Shared configuration for App Balloo project (backward compat layer)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rmdir /s /q dist 2>nul || del dist\\*.* 2>nul"
  },
  "keywords": ["config", "settings", "balloo"],
  "author": "NLP-Core-Team",
  "license": "MIT",
  "dependencies": {
    "@balloo/core-config": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.3.0"
  },
  "peerDependencies": {
    "dotenv": "^16.3.1"
  }
}
