{
  "name": "@balloo/core-config",
  "version": "0.1.0",
  "private": true,
  "description": "Core configuration - Extraction target from settings/",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "eslint src/"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  },
  "peerDependencies": {
    "dotenv": "^16.3.1"
  },
  "keywords": ["balloo", "config", "core"]
}